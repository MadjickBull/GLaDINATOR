/**
 * AI-related model classes.
 *
 * This package contains models related to the structured decision returned by the AI,
 * rather than to the mutable state of the game session itself.
 *
 * Main class:
 *
 * AiDecision
 * - represents the next decision produced by the AI;
 * - stores the type of decision to be taken;
 * - stores the textual content associated with that decision.
 *
 * Responsibility overview:
 * - this package models AI output;
 * - it does not store session state or game rules.
 *
 * -----------------------------------------------------------------------------
 *
 * Classes de modelo relacionadas com a IA.
 *
 * Este package contém modelos associados à decisão estruturada devolvida pela IA,
 * e não ao estado mutável da sessão de jogo.
 *
 * Classe principal:
 *
 * AiDecision
 * - representa a próxima decisão produzida pela IA;
 * - guarda o tipo de decisão a tomar;
 * - guarda o conteúdo textual associado a essa decisão.
 *
 * Em termos de responsabilidade:
 * - este package modela o output da IA;
 * - não guarda o estado da sessão nem as regras do jogo.
 */
package com.codeforall.online.gladinator.model.ia;
